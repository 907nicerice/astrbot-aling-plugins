# Configs

本目录只放示例配置和说明，不放真实部署配置。

真实配置应保存在 AstrBot 部署环境中，并由 `.gitignore` 排除。

## 示例规则

- 使用 `configs/examples/` 保存示例。
- 所有值必须是占位符。
- 不保存真实 QQ 账号、登录态、cookie、服务器地址、WebUI 密码或 provider 凭据。
- 插件目录内的真实 `config.json` 和 `*_config.json` 不进入仓库。

# Example environment file for local development notes only.
# Copy values from your own deployment environment when needed.
# Do not commit the real local .env file.

ASTRBOT_WORKDIR=/path/to/astrbot
ASTRBOT_PROVIDER_NAME=example-provider
ASTRBOT_MODEL_NAME=example-model
ALING_DASHBOARD_HOST=127.0.0.1
ALING_DASHBOARD_PORT=8787
